# OS / editor cruft
.DS_Store
.ipynb_checkpoints/
__pycache__/
*.pyc
*.pyo

# Editor / tool config
.claude/

# Local-only manuscript working files
paper/

# Local clone of the existing GitHub repo (for reference only)
github/

# Local-only planning notes
website_plan.md

# Heavy local-only working data (regenerated by scripts/, not for git)
data/                 # OpenAlex per-country pickles (~27 GB)
old/                  # original paper working tree archive
compact_data/         # Step 1 output (gzipped pickles with nqg labels)
web_data/             # Step 2 output (mirrored into docs/data/ for shipping)

# Pipeline log files
logs_*.txt
