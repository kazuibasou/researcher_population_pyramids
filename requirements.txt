# Python dependencies for the data pipeline in scripts/.
# Tested with Python 3.10+.
#
# The website itself (docs/) is plain HTML/CSS/JavaScript and has no
# Python dependencies; only the pipeline needs these.

nomquamgender>=0.1.4
